#include "digraph.h"
#include <stddef.h>

digraph *digraph_create(int n) {
  // digraph *g;
  // g = (digraph *)malloc(sizeof(digraph));
  // return g;
  return NULL;
}

void digraph_free(digraph *g) {}
